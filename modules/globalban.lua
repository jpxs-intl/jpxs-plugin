---@type Core
local Core = ...

-- currently not loaded by default, as its for people on the old ms lmfao
-- maybe at some point we can load it by default, but for now we just blacklist people

-- load banlist from gartbin
Core:loadGartbin("xvw4")
